CC := gcc
ifeq ($(compile), debug)
	CFLAGS := -O2 -Wall -g -std=c99
else
	CFLAGS := -O2 -Wall -std=c99
endif
LDFLAGS = -lm
.PHONY : subdirs $(SUBDIRS)
subdirs : $(SUBDIRS)
$(SUBDIRS) :
	$(MAKE) -C $@ all

%.o : %.c
	$(CC) $(CFLAGS) $(INCLUDES) -c $^ -o $(OBJDIRS)/$@

